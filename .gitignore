# compiled output
/dist
/node_modules

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# AgentMemory local storage
/data

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

/.gitmodules
.vscode
cursor

.env.dev
.env.prod
.env

# Deployment env files for docker stack deploy (./env/*.env beside the stack
# file). These hold real credentials and, unlike the files above, their names do
# NOT match the .env* rules — nothing else here would have stopped a commit.
/env/

# Debug
/3rd

# Temporary files
/tmp/*

# Terminal Paste Image folder
.cp-images/
.gitnexus

# playwright
.playwright-mcp

# agentmemory MCP local LMDB store (file-based, machine-local)
/data/
/prompt/
/design/
/docs/*
!/docs/images/
/features/
/enhancement/
/fixes/
/docs/*.md
/.claude/
/references/
.nodeterm

# --- Tracked exceptions: module documentation skills + their guide ---
# Everything under .claude stays local EXCEPT the per-module skills, so the
# team shares one source of truth for how each module works and relates.
!/.claude/
/.claude/*
!/.claude/skills/
/.claude/skills/*
!/.claude/skills/module-*/
!/.claude/skills/product-os-architecture/
# The generated reading guide + relationship map for those skills.
!/docs/module-skills/
# The prompt/decision history for this documentation work.
!/prompt/
/prompt/*
!/prompt/*_HISTORY.md


*/.nodeterm
# Scratch working notes from agent sessions — never part of the codebase.
.superpowers/
