# Prompt history — 2026-08-04

## 1. Đọc toàn bộ source, liệt kê module
> đọc toàn bộ source, và cho tao biết nó có những module nào ?

- Khảo sát monorepo: backend (NestJS/DDD/Mongo), frontend (React/Vite), saas-admin (React/Vite), collab (Yjs/Hocuspocus).
- Liệt kê ~26 backend domain module + feature FE + saas-admin + collab.

## 2. Vẽ sơ đồ quan hệ giữa các module
> vẽ sơ đồ quan hệ giữa các module

- Tạo Artifact với 5 sơ đồ mermaid: toàn cảnh 4 app, DDD 3 lớp, quan hệ domain (issue trung tâm), lớp nền tảng, bảng ánh xạ FE⇄BE.

## 3. Viết skill cho tất cả module + verify bằng 2 subagent
> viết toàn bộ skill cho tất cả module, và mối quan hệ liên quan giữa từng module, mỗi skill được viết và verify bằng 2 con subagent

Quyết định (qua AskUserQuestion):
- Định dạng: Claude Code project skill → `.claude/skills/module-<name>/SKILL.md`.
- Phạm vi: toàn bộ 4 app.
- Cách chạy: Workflow orchestration — pipeline mỗi module: 1 agent viết → 1 agent verify (đối chiếu source, sửa tại chỗ).

Sản phẩm: 35 skill module + 1 skill `product-os-architecture` (bản đồ quan hệ tổng).
Workflow run id: wf_de8f409e-ea8.
