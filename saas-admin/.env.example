# The product-hub API. The console talks to the same service the tenant app does,
# only under /v1/platform and with a platform token.
VITE_API_URL=http://localhost:3000/v1

# Where a "open workspace" link should send you. Optional — leave empty to hide
# those links.
VITE_APP_URL=http://localhost:3001
